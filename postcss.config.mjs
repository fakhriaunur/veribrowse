// Tailwind v4: the PostCSS plugin moved to `@tailwindcss/postcss`
// (lightningcss handles autoprefixing; the `autoprefixer` entry is dropped).
const config = {
  plugins: { "@tailwindcss/postcss": {} },
};
export default config;
